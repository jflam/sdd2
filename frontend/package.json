{
  "name": "safari-extension-unified-logging",
  "version": "1.0.1",
  "description": "Safari extension with unified logging system",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp dist/src/popup.html dist/popup.html && cp manifest.json dist/manifest.json",
    "preview": "vite preview",
    "test": "jest",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.0.4",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0"
  }
}
